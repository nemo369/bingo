import { Card } from '../types/card';
import { Condition } from '../types/game';

export const isCardValidate = (
  card: Card,
  conditions: Condition[]
): boolean => {
  // if(card === conditions){
  //     return false
  // }

  return false;
};
